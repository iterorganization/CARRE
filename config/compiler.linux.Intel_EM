FC		= ifort
CPP		= /lib/cpp -C -traditional

ifdef SOLPS_DEBUG
FFLAGS		= -g -O -C -traceback
else
FFLAGS		= -O3
endif

DBLPAD		= -align

COMPILE 	= ${FC} -c ${FFLAGS}

LDLIBS		= -L${SOLPS_LIB}
LDFLAGS 	= ${NCARG}
LDEXTRA 	= 
