FC		= g77
CPP		= /lib/cpp -C -traditional

ifdef SOLPS_DEBUG
FFLAGS		= -g
else
FFLAGS		= -O3
endif

COMPILE 	= ${FC} -c ${FFLAGS}

LDLIBS		= -L${SOLPS_LIB}
LDFLAGS 	= ${NCARG} # ${NCARG_ROOT}/BD/*.o 
LDEXTRA 	= 
