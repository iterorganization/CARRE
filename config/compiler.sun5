FC		= f77
CPP		= /usr/ccs/lib/cpp

ifdef SOLPS_DEBUG
FFLAGS		= -g -C 
else
FFLAGS		= -O
endif

DBLPAD		= -r8 -i4

COMPILE 	= ${FC} -c ${FFLAGS}

LDLIBS		= -L$${NCARG_ROOT}/lib -L${SOLPS_LIB}
LDFLAGS 	= -lncarg -lncarg_gks -lncarg_c -lX11 -lm -lmscl -lsocket
LDEXTRA 	= 
