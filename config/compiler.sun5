FC		= f77
FC		= f90
CPP		= /usr/ccs/lib/cpp

ifdef SOLPS_DEBUG
FFLAGS		= -g -C 
else
FFLAGS		= -O
endif

DBLPAD		= -r8 -i4
DBLPAD		= -xtypemap=real:64,double:64,integer:32

COMPILE 	= ${FC} -c ${FFLAGS}

LDLIBS		= -L${SOLPS_LIB}
LDFLAGS 	= ${NCARG} -lsocket
LDEXTRA 	= 
LDEXTRA 	= -lf77compat
