FC		= g95
CPP		= /lib/cpp -C -traditional

ifdef SOLPS_DEBUG
FFLAGS		= -g -C -fbounds-check -ftrace=full
else
FFLAGS		= -g -O
endif

DBLPAD		= -r8 

COMPILE 	= ${FC} -c ${FFLAGS}

LDLIBS		= -L${SOLPS_LIB}
LDFLAGS 	= ${NCARG} -lmscl
LDEXTRA 	= 
