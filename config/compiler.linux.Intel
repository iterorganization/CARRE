FC		= f95i
CPP		= /lib/cpp -C -traditional

ifdef SOLPS_DEBUG
FFLAGS		= -g -O -C
else
FFLAGS		= -O3 -tpp7
endif

DBLPAD		= -Ad 

COMPILE 	= ${FC} -c ${FFLAGS}

LDLIBS		= -L${SOLPS_LIB}
LDFLAGS 	= ${NCARG} -lmscl
LDEXTRA 	= 
