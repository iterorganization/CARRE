FC		= pgf90
CPP		= /lib/cpp -traditional

ifdef SOLPS_DEBUG
FFLAGS		= -g -C 
else
FFLAGS		= -O
endif

DBLPAD		= -r8 -i4

COMPILE 	= ${FC} -c ${FFLAGS}

LDLIBS		= -L$${NCARG_ROOT}/lib -L$(SOLPSTOP)/src/lib/$(OBJECTCODE)
LDFLAGS 	= -lncarg -lncarg_gks -lncarg_c -L/usr/X11R6/lib -lX11 -lm -lmscl
LDEXTRA 	= 
